/**
 * 애플리케이션 실행 환경과 보조 설정을 담는 패키지다.
 *
 * <p>현재는 기존 파일 기반 H2 데이터베이스를 새 엔티티 구조와 맞추기 위한
 * 마이그레이션 코드가 있다. JPA의 자동 DDL만으로 처리하기 어려운 컬럼 추가나
 * 기존 데이터 보정은 이 계층에서 애플리케이션 시작 시점에 수행한다.</p>
 */
package com.game.buildingstory.config;
